<Window x:Class="DownloaderAppWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloaderAppWpf"
        mc:Ignorable="d"
        Title="Windows Downloader" Height="620" Width="960">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Windows Downloader" FontSize="28" FontWeight="Bold"/>

            <!-- Registering the native host is what lets the browser extension reach the
                 downloader, so a failure must be visible rather than hidden in the title bar. -->
            <Border x:Name="RegistrationBanner" Visibility="Collapsed" Margin="0,10,0,0"
                    Background="#FDECEA" BorderBrush="#E5A5A0" BorderThickness="1"
                    CornerRadius="4" Padding="10,7">
                <TextBlock x:Name="RegistrationBannerText" Foreground="#8C1D18" TextWrapping="Wrap"/>
            </Border>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="UrlTextBox" Grid.Column="0" Margin="0,0,10,0" FontSize="14" VerticalContentAlignment="Center" Height="36" ToolTip="https://..."/>
            <Button Grid.Column="1" Content="Add" Width="90" Height="36" Click="AddDownload_Click" Margin="0,0,10,0"/>
            <Button Grid.Column="2" Content="Choose Folder" Width="120" Height="36" Click="ChooseFolder_Click"/>
        </Grid>

        <!-- Read from disk by the host, so it survives an app restart or a crash. Hidden when
             there is nothing to resume. -->
        <StackPanel x:Name="UnfinishedPanel" Grid.Row="2" Margin="0,0,0,14" Visibility="Collapsed">
            <TextBlock Text="Unfinished downloads" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <DataGrid x:Name="PartialsGrid" ItemsSource="{Binding}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                      MaxHeight="150" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding FileName}" Width="*"/>
                    <DataGridTextColumn Header="Downloaded" Binding="{Binding DownloadedText}" Width="120"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding SizeText}" Width="110"/>
                    <DataGridTextColumn Header="Tier" Binding="{Binding Tier}" Width="150"/>
                    <DataGridTemplateColumn Header="" Width="170">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="{Binding ActionText}" ToolTip="{Binding ActionHint}"
                                            Padding="10,2" Margin="0,0,6,0" Click="ResumeRow_Click"/>
                                    <Button Content="Discard" Padding="10,2" Click="DiscardRow_Click"
                                            ToolTip="Delete the partial file and its resume data"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <DataGrid x:Name="DownloadsGrid" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>
                <DataGridTextColumn Header="Progress" Binding="{Binding ProgressText}" Width="100"/>
                <DataGridTextColumn Header="Downloaded" Binding="{Binding OffsetText}" Width="120"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeText}" Width="110"/>
                <DataGridTextColumn Header="Tier" Binding="{Binding TierText}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <!-- The same explanation the extension gives, on hover. -->
                            <Setter Property="ToolTip" Value="{Binding TierHint}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Details" Binding="{Binding Message}" Width="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding Message}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasFailed}" Value="True">
                                    <Setter Property="Foreground" Value="#C5221F"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <!-- Pause appears only where the server honours ranges. On a
                                     non-resumable source it would mean discarding everything
                                     downloaded so far, so only Cancel is offered there. -->
                                <Button Content="Pause" Padding="10,2" Margin="0,0,6,0"
                                        Click="PauseRow_Click"
                                        Visibility="{Binding CanPause, Converter={StaticResource BoolToVis}}"/>
                                <Button Content="Cancel" Padding="10,2"
                                        Click="CancelRow_Click"
                                        IsEnabled="{Binding CanCancel}"
                                        Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="EmptyHint" Grid.Row="3" Foreground="#888" FontSize="14"
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                   Text="No downloads yet.&#10;Paste a link above, or use the browser extension."/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="#555" Text="Ready"/>
            <TextBlock x:Name="FolderText" Grid.Column="1" Foreground="#777" HorizontalAlignment="Right"
                       TextTrimming="CharacterEllipsis" Text=""/>
        </Grid>
    </Grid>
</Window>
